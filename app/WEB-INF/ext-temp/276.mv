select sum(a0.JE) sum_1,a1.DEPT_ID as a1_DEPT_ID,a1.DEPT_NAME as a1_DEPT_NAME,a0.RQ as a0_RQ  from MX_DATAS_ar a0 join DEPT a1 on a0.JGID=a1.DEPT_ID  where 1=1  and a1.lv2_id = '0010102'  and a1.DEPT_ID in ('001010202') group by a1.DEPT_ID,a1.DEPT_NAME,a0.RQ
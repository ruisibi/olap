select a1.DEPT_ID a1_DEPT_ID,a1.DEPT_NAME a1_DEPT_NAME,sum(a0.JE) sum_1  from MX_DATAS_ar a0 join DEPT a1 on a0.JGID=a1.DEPT_ID  left join CODE_AREA a2 on a0.AREA=a2.ID  where 1=1  and a1.lv2_id = '0010102'  and a0.RQ in ('2013-04-01') and a2.ID in (7) group by a1.DEPT_ID,a1.DEPT_NAME